#ifndef UD_POINTER_H
# define UD_POINTER_H

// Lib
#include <stdlib.h>

// Macro

// Structures

// Prototypes
size_t      ud_ptr_len(void **ptr);
void        **ud_ptr_init(size_t len);

#endif
